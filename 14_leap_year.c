// to check if the year entered is leap year

#include <stdio.h> 

int main(void)
{
    int yr;
    printf("Enter a year: ");
    scanf("%d", yr);
    //if (yr % 1.25 == 0)
    //    printf("%d is a leap year.\n", yr);
    //else
    //    printf("%d is not a leap year.\n", yr);
    
    return 0;
}